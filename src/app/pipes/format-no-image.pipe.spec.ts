import { FormatNoImagePipe } from './format-no-image.pipe';

describe('FormatNoImagePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNoImagePipe();
    expect(pipe).toBeTruthy();
  });
});
